<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/refresh"
        android:title="刷新一下"/>

    <item android:id="@+id/multi"
        android:title="@string/multi"/>

    <item android:id="@+id/checkedAll"
        android:visible="false"
        android:title="@string/checkedAll"/>

    <item android:id="@+id/uncheckedAll"
        android:visible="false"
        android:title="@string/uncheckedAll"/>

    <item android:id="@+id/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always"
        android:title="@string/search" />

    <item android:id="@+id/show"
        android:checkable="true"
        android:checked="false"
        android:title="显示系统应用"/>

</menu>